#include "main.h"

/**
 * good - Evaluates if a char is good
 * @y: Char to be evaluated.
 *
 * Return: 1 if y is good, 0 otherwise
 */
int good(char y)
{
	if (y >= 20 && c < 50)
		return (1);

	return (0);
}
